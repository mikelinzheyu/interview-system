# 🚀 AI面试系统 - 快速启动指南

## ✅ 已完成的步骤
- [x] **阶段1**: 后端Java编译成功 ✓
  - JAR文件: `backend-java/target/interview-server.jar` (44MB)
  - Spring Boot 3.2.0 + Java 17
  
- [x] **前端**: 开发服务器已运行
  - Frontend: http://localhost:5174
  - Mock API: http://localhost:3001

## ⏳ 接下来需要做的

### 方案 A: 使用 Docker（推荐）
最简单的方式，所有依赖自动配置。

```bash
# 进入项目目录
cd D:/code7/interview-system

# 启动所有服务（MySQL + Redis + 后端）
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down
```

**访问地址:**
- 前端: http://localhost
- 后端API: http://localhost:8080/api/v1
- 健康检查: http://localhost:8080/api/v1/actuator/health

---

### 方案 B: 本地运行（需要预装MySQL和Redis）

**前提条件:**
1. MySQL 5.7+ 运行在 localhost:3306
   - 数据库: `interview_system`
   - 用户: `root`
   - 密码: `123456`

2. Redis 运行在 localhost:6379

**启动步骤:**

```bash
# 1. 启动后端（在新终端）
cd D:/code7/interview-system/backend-java
java -Xms512m -Xmx1024m \
  -Dspring.datasource.password=123456 \
  -jar target/interview-server.jar

# 2. 启动前端（在新终端）
cd D:/code7/interview-system/frontend
npm run dev
```

**访问地址:**
- 前端: http://localhost:5174
- 后端API: http://localhost:8080/api/v1

---

## 🗄️ 数据库迁移

后端启动时会自动执行数据库迁移（使用Flyway）。

**迁移文件位置:**
```
backend-java/src/main/resources/db/migration/
```

**如需手动迁移，执行:**
```sql
ALTER TABLE questions ADD COLUMN major_group_id BIGINT AFTER category_id;
ALTER TABLE questions ADD INDEX idx_major_group_id (major_group_id);
```

---

## 🔧 配置说明

### 后端配置文件
```
backend-java/src/main/resources/application.yml
```

**关键配置:**
- 服务端口: 8080
- 上下文路径: /api/v1
- MySQL: localhost:3306
- Redis: localhost:6379
- JWT密钥: 可通过环境变量JWT_SECRET覆盖

### 环境变量

可通过以下环境变量覆盖配置：

```bash
DB_PASSWORD=your_password          # MySQL密码
REDIS_PASSWORD=your_password       # Redis密码
JWT_SECRET=your_secret_key         # JWT密钥
OPENAI_API_KEY=sk-xxx              # OpenAI API密钥
```

---

## 📊 系统架构

```
┌─────────────────────────────────────────────┐
│         用户浏览器 (http://localhost)        │
└─────────────────┬───────────────────────────┘
                  │
        ┌─────────┴──────────┐
        │                    │
    ┌───▼──────────┐  ┌─────▼─────────┐
    │ 前端服务(80) │  │ Nginx(443)    │
    └───┬──────────┘  └─────┬─────────┘
        │                    │
        │    ┌───────────────┘
        │    │
    ┌───▼────▼──────────┐
    │  后端API(8080)    │
    │  Java Spring Boot │
    └───┬────┬──────────┘
        │    │
   ┌────▼──┐│
   │        │
┌──▼──────┐ ▼──────────┐
│  MySQL  │   Redis    │
│  (3306) │   (6379)   │
└─────────┴────────────┘
```

---

## ✨ 主要功能

- ✅ AI面试官（对话系统）
- ✅ 题目库管理
- ✅ 错题记录和分析
- ✅ 用户认证和授权
- ✅ WebSocket实时通知
- ✅ 多维度数据统计

---

## 🐛 常见问题

**Q: 后端启动报错 "Connection refused"**
A: 确保MySQL和Redis已启动。使用Docker方案可自动解决。

**Q: 前端无法连接后端**
A: 检查后端是否运行在8080端口。确保CORS配置正确。

**Q: 数据库迁移失败**
A: 检查MySQL连接信息。确保有正确的权限创建表。

---

## 📞 获取帮助

查看日志文件：
```bash
# Docker日志
docker-compose logs interview-backend

# 本地日志
tail -f logs/interview-backend.log
```

