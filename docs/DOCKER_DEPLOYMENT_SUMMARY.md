# AI面试系统 - Docker生产环境部署完整总结

## 📦 部署内容清单

本部署包已为AI面试系统准备了完整的Docker生产环境部署方案，包括以下所有文件和配置：

### 1️⃣ 部署脚本

| 文件 | 功能 | 使用方式 |
|------|------|--------|
| `deploy-prod.sh` | Linux/Mac一键部署脚本 | `chmod +x deploy-prod.sh && ./deploy-prod.sh` |
| `deploy-prod.bat` | Windows一键部署脚本 | 右键管理员运行或命令行执行 |
| `backup-prod.sh` | 生产环境备份脚本 | `./backup-prod.sh` |
| `restore-backup.sh` | 数据恢复脚本 | `./restore-backup.sh [backup_file]` |

### 2️⃣ Docker配置文件

| 文件 | 用途 |
|------|------|
| `docker-compose.yml` | 主要服务配置（后端、前端、Redis） |
| `docker-compose-monitoring.yml` | 监控服务配置（Prometheus、Grafana、Loki） |
| `.env.docker` | 环境变量配置（需要填充敏感信息） |

### 3️⃣ 监控配置

| 文件 | 功能 |
|------|------|
| `monitoring/prometheus.yml` | Prometheus指标收集配置 |
| `monitoring/alert-rules.yml` | 告警规则定义 |
| `monitoring/alertmanager.yml` | 告警管理和通知配置 |
| `monitoring/loki-config.yml` | 日志聚合配置 |
| `monitoring/promtail-config.yml` | 日志收集代理配置 |

### 4️⃣ 文档

| 文档 | 内容 |
|------|------|
| `PRODUCTION_DEPLOYMENT_GUIDE.md` | 完整的部署指南（240页详细说明） |
| `DOCKER_DEPLOYMENT_QUICK_START.md` | 快速启动指南（5分钟快速部署） |
| `DOCKER_DEPLOYMENT_SUMMARY.md` | 本文件 |

---

## 🎯 部署架构

```
┌─────────────────────────────────────────────────────────────┐
│                    生产环境完整架构                         │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │           Nginx 反向代理 (可选)                       │   │
│  │  - SSL/TLS终止  - 负载均衡  - 请求日志              │   │
│  └─────────────────────────────────────────────────────┘   │
│                          ↓                                   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │         Docker Bridge Network                        │   │
│  │                                                      │   │
│  │  ┌──────────────┐  ┌────────────────┐  ┌──────────┐ │   │
│  │  │   Frontend   │  │   Backend API  │  │  Redis   │ │   │
│  │  │   (Nginx)    │  │   (Node.js)    │  │ (Cache)  │ │   │
│  │  └──────────────┘  └────────────────┘  └──────────┘ │   │
│  │                                                      │   │
│  │  ┌─────────────────────────────────────────────┐    │   │
│  │  │    监控和日志 (可选)                         │    │   │
│  │  │  - Prometheus  - Grafana  - Loki/Promtail │    │   │
│  │  └─────────────────────────────────────────────┘    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │         数据卷 (Volumes)                             │   │
│  │  - Redis数据  - 上传文件  - 日志  - 配置             │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 🚀 快速开始

### 最快的启动方式（3分钟）

```bash
# 1. 准备环境
cp .env.docker.example .env.docker
nano .env.docker  # 编辑DIFY_API_KEY等关键信息

# 2. 创建目录
mkdir -p logs/{backend,frontend,redis,proxy} \
         data/{redis,uploads}

# 3. 启动服务
docker-compose up -d

# 4. 验证
docker-compose ps
curl http://localhost:80
```

### 使用部署脚本（更安全的方式）

```bash
# Linux/Mac
chmod +x deploy-prod.sh
./deploy-prod.sh

# Windows
deploy-prod.bat
```

---

## 📋 必需的环境变量

**文件: `.env.docker`**

```env
# 必须修改（从Dify获取）
DIFY_API_KEY=app-your-api-key-here
DIFY_API_BASE_URL=https://api.dify.ai/v1

# 必须修改（设置强密钥）
JWT_SECRET=your-super-strong-secret-key

# 可选修改（默认值已适合大多数场景）
FRONTEND_PORT=80
BACKEND_PORT=8080
REDIS_PORT=6379
TZ=Asia/Shanghai
```

---

## ✅ 部署检查清单

### 部署前检查

- [ ] 安装了Docker 20.10+
- [ ] 安装了Docker Compose 2.0+
- [ ] 有至少4GB可用内存
- [ ] 有至少10GB可用磁盘空间
- [ ] 获取了Dify API密钥
- [ ] 生成了强JWT密钥

### 部署步骤

- [ ] 复制`.env.docker`并填充所有必需的环境变量
- [ ] 创建了必要的目录结构
- [ ] 运行了部署脚本或手动启动了容器
- [ ] 验证了所有容器都在运行
- [ ] 测试了前端、后端和Redis连接

### 部署后检查

- [ ] 前端可以访问（http://localhost:80）
- [ ] 后端API健康检查通过（http://localhost:8080/api/health）
- [ ] Redis连接成功（`docker-compose exec redis redis-cli ping`）
- [ ] 日志目录已创建并有日志输出
- [ ] 配置了备份计划

---

## 🔧 常用命令参考

### 容器管理

```bash
# 查看所有容器状态
docker-compose ps

# 查看实时日志
docker-compose logs -f

# 重启特定服务
docker-compose restart backend

# 停止所有服务
docker-compose down

# 启动所有服务
docker-compose up -d
```

### 备份和恢复

```bash
# 创建备份
./backup-prod.sh

# 从最新备份恢复
./restore-backup.sh

# 从特定备份恢复
./restore-backup.sh backup_20240101_120000.tar.gz
```

### 调试和诊断

```bash
# 进入后端容器
docker-compose exec backend sh

# 查看后端日志
docker-compose logs -f backend

# 查看Redis状态
docker-compose exec redis redis-cli INFO

# 检查网络连接
docker-compose exec backend ping interview-redis
```

### 监控（如果启用了monitoring）

```bash
# 启用监控服务
docker-compose -f docker-compose.yml \
               -f docker-compose-monitoring.yml up -d

# 访问Grafana仪表板
# http://localhost:3000 (用户: admin, 密码: admin123)

# 访问Prometheus
# http://localhost:9090

# 访问Alertmanager
# http://localhost:9093
```

---

## 🔐 安全建议

### 部署前

1. **修改所有密钥**
   ```bash
   # 生成强密钥
   openssl rand -base64 32
   ```

2. **更新Docker镜像**
   ```bash
   docker-compose pull
   ```

3. **审查环境变量**
   - 不要提交`.env.docker`到版本控制
   - 添加到`.gitignore`

### 部署后

1. **启用SSL/TLS**
   ```bash
   # 使用Let's Encrypt
   sudo certbot certonly --standalone -d your-domain.com
   ```

2. **配置防火墙**
   ```bash
   # 仅允许必需的端口
   sudo ufw allow 80/tcp
   sudo ufw allow 443/tcp
   ```

3. **定期备份**
   ```bash
   # 设置定时备份
   0 2 * * * /path/to/backup-prod.sh
   ```

4. **监控日志**
   ```bash
   # 定期检查错误日志
   docker-compose logs backend | grep ERROR
   ```

---

## 📊 监控配置（可选但推荐）

### 启用监控

```bash
# 使用扩展配置启动监控服务
docker-compose -f docker-compose.yml \
               -f docker-compose-monitoring.yml up -d
```

### 监控组件

- **Prometheus**: 时序数据库，收集系统和应用指标
- **Grafana**: 数据可视化，创建仪表板和图表
- **Loki**: 日志聚合系统
- **Alertmanager**: 告警管理和通知
- **cAdvisor**: 容器资源监控
- **Node Exporter**: 宿主机指标收集

### 监控仪表板

- Grafana: http://localhost:3000
- Prometheus: http://localhost:9090
- Alertmanager: http://localhost:9093

---

## 💾 备份策略

### 自动备份

```bash
# 配置定时备份（crontab）
# 每天凌晨2点执行
0 2 * * * /path/to/interview-system/backup-prod.sh
```

### 备份内容

- Redis数据库（RDB和AOF）
- 用户上传文件
- 应用配置文件
- 应用日志
- 数据库备份（如果有）

### 备份保留

- 默认保留30天
- 自动压缩和存档
- 支持上传到S3或远程存储

---

## 🐛 常见问题

### Q: 如何修改前端/后端端口？
A: 编辑`.env.docker`，修改`FRONTEND_PORT`和`BACKEND_PORT`，然后重启：
```bash
docker-compose down
docker-compose up -d
```

### Q: Redis内存爆满怎么办？
A: 增加Redis最大内存：
```bash
# 编辑docker-compose.yml中的Redis配置
--maxmemory 1gb
```

### Q: 如何查看应用日志？
A:
```bash
# 查看所有日志
docker-compose logs -f

# 查看特定服务
docker-compose logs -f backend
```

### Q: 如何恢复备份？
A:
```bash
./restore-backup.sh                              # 恢复最新备份
./restore-backup.sh backup_20240101_120000.tar.gz  # 恢复特定备份
```

### Q: 如何升级到新版本？
A:
```bash
# 备份当前数据
./backup-prod.sh

# 拉取新镜像
docker-compose pull

# 重新启动
docker-compose down
docker-compose up -d
```

---

## 📚 文档导航

| 文档 | 用途 | 读者 |
|------|------|------|
| `DOCKER_DEPLOYMENT_QUICK_START.md` | 5分钟快速启动 | 新用户、快速部署 |
| `PRODUCTION_DEPLOYMENT_GUIDE.md` | 完整部署指南 | 运维工程师、深入学习 |
| `DOCKER_DEPLOYMENT_SUMMARY.md` | 本文（总结概览） | 项目经理、快速了解 |

---

## 🎓 学习资源

- [Docker官方文档](https://docs.docker.com)
- [Docker Compose用户指南](https://docs.docker.com/compose/compose-file/)
- [Docker最佳实践](https://docs.docker.com/develop/dev-best-practices/)
- [Prometheus文档](https://prometheus.io/docs)
- [Grafana入门教程](https://grafana.com/tutorials)
- [Dify官方文档](https://docs.dify.ai)

---

## 📞 支持和反馈

### 遇到问题时：

1. **查看日志**
   ```bash
   docker-compose logs -f
   ```

2. **检查容器状态**
   ```bash
   docker-compose ps
   ```

3. **测试连接**
   ```bash
   docker-compose exec backend curl http://localhost:3001/api/health
   docker-compose exec redis redis-cli ping
   ```

4. **查看文档**
   - 快速问题: `DOCKER_DEPLOYMENT_QUICK_START.md`
   - 详细问题: `PRODUCTION_DEPLOYMENT_GUIDE.md`

---

## 📝 版本信息

| 组件 | 版本 | 状态 |
|------|------|------|
| Docker | 20.10+ | ✅ |
| Docker Compose | 2.0+ | ✅ |
| Node.js | 18-alpine | ✅ |
| Nginx | 1.25-alpine | ✅ |
| Redis | 7-alpine | ✅ |
| Prometheus | 2.45+ | ✅ (可选) |
| Grafana | 10.0+ | ✅ (可选) |
| Loki | 2.9+ | ✅ (可选) |

---

## ✨ 主要特性

✅ **一键部署** - 提供自动化脚本，无需复杂配置
✅ **监控告警** - 集成Prometheus、Grafana和Alertmanager
✅ **日志聚合** - 使用Loki和Promtail收集所有日志
✅ **备份恢复** - 自动备份和恢复数据
✅ **健康检查** - 所有服务都配置了健康检查
✅ **安全最佳实践** - 遵循Docker安全指南
✅ **性能优化** - 包含资源限制和缓存配置
✅ **完整文档** - 提供详细的部署和运维指南

---

## 🎯 后续步骤

1. **部署系统**
   - 按照`DOCKER_DEPLOYMENT_QUICK_START.md`快速部署

2. **配置监控**（可选）
   - 启用监控组件进行实时监控和告警

3. **设置备份**
   - 配置自动备份计划确保数据安全

4. **性能调优**
   - 根据实际情况调整资源限制和缓存大小

5. **安全加固**
   - 修改所有默认密钥
   - 启用SSL/TLS
   - 配置防火墙

---

## 📄 许可证

MIT License

---

**版本**: 1.0
**最后更新**: 2024年01月
**维护者**: AI面试系统团队

---

## 快速链接

- 🚀 [快速开始](./DOCKER_DEPLOYMENT_QUICK_START.md)
- 📖 [完整指南](./PRODUCTION_DEPLOYMENT_GUIDE.md)
- 📝 [部署脚本](./deploy-prod.sh)
- 💾 [备份脚本](./backup-prod.sh)
- ♻️ [恢复脚本](./restore-backup.sh)

---

**祝你部署愉快！** 🎉
